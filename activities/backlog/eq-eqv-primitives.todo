Activity target: 
  Implement the basic predicates eq? and eqv?. At this point, eq? and eqv?
  behave exactly the same, so it's really only a matter of implementing one.

Activity number: xxx

Activity start:  xxxx-xx-xx xx:xx
Activity finish: xxxx-xx-xx xx:xx

Number of work-hours
  Expected: 0.25
  Realized: x

Current Activity Tasks: 
  - Create function for eq?
  - Bind the function as eq? and eqv? on the repl toplevel environment.

